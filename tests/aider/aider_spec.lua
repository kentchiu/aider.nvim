local plugin = require("aider.terminal_events")

describe("setup", function()
  it("Test should always pass", function()
    assert("Hello!" == "Hello!", "Only for ensure testing is setup well")
  end)
end)
